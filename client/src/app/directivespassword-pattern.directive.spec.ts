import { DirectivespasswordPatternDirective } from './directivespassword-pattern.directive';

describe('DirectivespasswordPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivespasswordPatternDirective();
    expect(directive).toBeTruthy();
  });
});
